# 의존성 폴더 (가장 중요)
node_modules/
pnpm-lock.yaml
yarn.lock

# Next.js 빌드 결과물
.next/
out/
build/

# 환경변수 파일 (보안상 업로드 금지)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 로그 파일
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS 시스템 파일
.DS_Store
Thumbs.db